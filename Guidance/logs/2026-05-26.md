# 2026-05-26 会话日志

## 质检兜底 + 外部链接后端方案
**产出**: `demo/server/capture.js`, `ai.js`, `src-tauri/src/main.rs`, `demo/web/src/App.tsx`
**关键结论**: sanitizeContent() 7步正则在三层链路复用（capture->Pipeline->前端），作为Prompt约束之外的兜底；外部链接走后端端点 POST /api/open-url
**关联**: E-025, E-024

## 分类器三层修复 + 卡片类型手动切换
**产出**: classifier prompt 二次重写, `src-tauri/src/main.rs`, `App.tsx`
**关键结论**: 最关键根因：few-shot示例放在 ## 示例输出 之下，extract_prompt_block提取范围不含此节，13个few-shot从未发送给LLM；Prompt 300->110行决策树 + intent_by_key中英文双向匹配
**关联**: E-023, P-007, P-008

## Tauri 1:1 复刻 Demo 数据链路
**产出**: `src-tauri/src/main.rs` (~600行新增), `schema.sql`, `demo/extension/content.js`, 5份 Guidance
**关键结论**: 4步清洗函数(normalize_role/clean_content/merge_consecutive/clean_conversation)完整移植到Rust；HTTP Router从2条补全到11条；FTS从content外部表改为独立表
**关联**: E-021, E-018

## 组件 remount 根因 + 3列布局修复
**产出**: `demo/web/src/App.tsx`, `index.css`, SVG assets x3, `tauri.conf.json`, 4份 Guidance
**关键结论**: React内联组件+父setter->remount->local state丢失(P-010)；3列布局: 窗口1440+侧边栏260+网格minmax(230px,1fr)
**关联**: E-027, P-010
